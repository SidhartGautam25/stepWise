The terminal is a text interface for controlling a computer. In this lesson, every command updates the visualizer so you can connect the command with the filesystem change.

You will start in `/home/student`, which is the home directory for the `student` user.
