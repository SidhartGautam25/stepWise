# Moving Between Directories

Now use the `projects` directory you created in the previous step.

Complete this activity:

```bash
cd projects
pwd
cd ..
cd projects
```

The final current directory should be `/home/student/projects`.
