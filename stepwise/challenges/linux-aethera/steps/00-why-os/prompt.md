# Why an operating system exists

This step is still visual and interactive.

Move through the lesson to understand:

- what the main hardware parts are doing
- why raw hardware is hard to use directly
- how an operating system coordinates CPU, memory, and storage
- why storage becomes files and directories instead of raw bits

Use the **Next** button in the visual lesson to continue.
